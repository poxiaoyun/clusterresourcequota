apiVersion: v2
name: clusterresourcequota
description: A Helm chart for ClusterResourceQuota Admission
version: 0.0.0 # This will be replaced by the actual version during packaging.
appVersion: v0.0.0 # This will be replaced by the actual git version during packaging.
dependencies:
  - name: common
    version: 2.x.x
    repository: oci://registry-1.docker.io/bitnamicharts
